# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/nurturers
PGHOST=localhost
PGPORT=5432
PGDATABASE=nurturers
PGUSER=postgres
PGPASSWORD=password
PGSSL=false

# Server Configuration
PORT=5000
FRONTEND_URL=http://localhost:3000
NODE_ENV=development

# JWT Secret
JWT_SECRET=maseno-counseling-super-secret-jwt-key-2024-change-in-production

# Bot Configuration (optional for local development)
BOT_TOKEN=

# Debug Settings
DEBUG_DB=false
